const ConditionalRendering = () => {
  // # If condition
  // const isPremium = false

  // let isGraduated = false
  // let isPlaced = false

  // let knowsReact = false
  // let knowsNode = false

  // # If else
  // const isEligibileToVote = true

  // # If - else if - else / Nested if else
  // let isPrime = false
  // let doneTxnsGrtrThn5000 = false

  return (
    <>
      {/* # If condition */}
      {/* {isPremium && (
        <img src='https://static.vecteezy.com/system/resources/previews/010/354/208/non_2x/sign-of-premium-symbol-is-isolated-on-a-white-background-icon-color-editable-free-vector.jpg' alt='premium' width={200} />   
      )} */}
      {/* {isGraduated && isPlaced && <p>The student has graduated and has been placed as well</p>} */}
      {/* {(knowsReact || knowsNode) && <p>The student is shortlisted</p>} */}

      {/* # If else condition */}
      {/* {isEligibileToVote ? (
        <h1>You are eligible to vote</h1>
      ) : (
        <h1>You are not eligible to vote</h1>
      )} */}

      {/* # If - else if - else / Nested if else */}
      {/* {isPrime ? (
        doneTxnsGrtrThn5000 ? (
          <p>You are a Prime member and have done transactions greater than 5000</p>
        ) : (
          <p>You are a Prime member but your transaction amount is less than 5000</p>
        )        
      ) : (
        <p>You are not a Prime member</p>
      )} */}
    </>
  )
}

export default ConditionalRendering